[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "harmonydagger"
version = "0.4.0"
description = "A tool for protecting audio against use in AI training"
readme = "README.md"
requires-python = ">=3.9"
license = {file = "LICENSE"}
authors = [
    {name = "Jascha Wanger"}
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Sound/Audio",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.20.0",
    "scipy>=1.7.0",
    "librosa>=0.9.0",
    "soundfile>=0.10.0",
    "tqdm>=4.60.0",
    "matplotlib>=3.4.0",
    "pydub>=0.25.1",  # For MP3 writing support
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=3.0.0",
    "black>=23.0.0",
    "isort>=5.10.0",
    "mypy>=0.910",
    "flake8>=4.0.0",
    "twine>=4.0.0",
]
docs = [
    "sphinx>=4.5.0",
    "sphinx-rtd-theme>=1.0.0",
]
gpu = [
    "torch>=2.0.0",
]
streamlit = [
    "streamlit>=1.20.0",
]
music = [
    # NOTE: mutually exclusive with the `clone` extra below, which pins transformers<5.
    # The research code needs transformers>=5 (SDPA/eager handling, ClapProcessor kwargs).
    "torch>=2.0.0",
    "torchaudio>=2.0.0",
    "transformers>=5.0",
    "peft>=0.10.0",
    "accelerate>=0.30.0",
    "librosa>=0.10.0",
    "pandas>=2.0.0",
]
clone = [
    "coqui-tts[codec]>=0.24.0",
    "torch>=2.0.0",
    "torchaudio>=2.0.0",
    "transformers>=4.57,<5",
]

[project.urls]
"Homepage" = "https://github.com/jaschadub/harmonydagger"
"Bug Tracker" = "https://github.com/jaschadub/harmonydagger/issues"

[project.scripts]
harmonydagger = "harmonydagger.cli:main"

[tool.setuptools]
packages = ["harmonydagger", "research"]

[tool.black]
line-length = 88
target-version = ['py39', 'py310', 'py311', 'py312']
include = '\.pyi?$'

[tool.isort]
profile = "black"
line_length = 88
